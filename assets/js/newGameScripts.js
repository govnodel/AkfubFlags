//fill flags first time
    //1) choose 4 random flags from list
    //2) set random position
    //3) set name of country
    //4) add event listeners
    //5) set timer
//set all elements' opacity to 1
//start timer
//waiting clicks ========================
//check answer
    //1) right or not?
    //2) final or not?
        //exit if lose
//refresh
//waiting timer ========================
//decrease lives
//check final or not?
    //exit if lose
//refresh

var names = [];
var flags = [];

function setFlags(){
  let numbers = [];
  for (let i = 0; i < 4; i++){
    let num = Math.round(Math.random() * (names.length - 1));
    while (num != ) {

    }
  }
}
